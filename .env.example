# LLM Judge — provide ONE of these (or neither, rule-based fallback kicks in)
OPENAI_API_KEY=your_openai_api_key_here   # option 1: OpenAI (paid)
GROQ_API_KEY=your_groq_api_key_here       # option 2: Groq — free tier, get at console.groq.com


## Create .env file and note this file shall be remain private not to be pushed in repo.

## DO not make any changes in this file